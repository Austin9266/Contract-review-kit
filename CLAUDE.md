# CLAUDE.md —— 这套工具包怎么改、怎么长

这个文件同时写给两个读者：**在这个目录里工作的 Claude**，和**要把它调教成自己那套的人**。
上手用请看 `README.md`；这里讲的是维护——尤其是规则卡怎么加、怎么改、怎么不改坏。

一句话：**代码基本不用动，要长的是规则卡。**

---

## 0. 一分钟看懂结构

```
配置.json                 署名、时段、交付后缀 —— 装完先改这个
CLAUDE.md                 你正在看的这份
README.md                 给使用者的上手说明
脱敏自检.py               ★ 发布/分享前必跑，按特征查九类泄露（含自查）
脱敏词表.local.txt        你的机构名与同事名，自己建、不进仓库（自检读它做 L4）
脱敏白名单.txt            L8 的放行清单（只对 L8 生效）
发布到GitHub.sh           开源发布（内置脱敏自检做闸门）
skills/
  contract-review-sop/    主技能：判断标准 + 全部程序，自足
    SKILL.md              流水线本身（0–7 步）
    rules/规则库.json      ★ 138 张规则卡 —— 这套东西的心脏
    rules/识别线索.json    机械触发词：什么词出现就"像"哪个包
    rules/易混词.json      错别字判词表
    scripts/assemble.py   按本件情况装配当次审查指令（工作台的无界面版）
    scripts/查规则库.py    ★ 规则库体检 —— 改完卡片必跑
    scripts/oneshot.py    落盘引擎入口
    scripts/typo.py       错别字机械扫描
    scripts/抽取工作台.py  把工作台 HTML 里的规则与文本积木抽回技能
  contract-redline/       子技能：只落盘
  contract-cross-reference-checker/  子技能：只查交叉引用
工作台/审查工作台.html      有界面地改卡片、生成审查 prompt（离线可用）
落盘台/                    有界面地落盘 + 无界面的 一键落盘.py
```

**同一份东西有两处副本**，这是有意的（技能要能被拷到 `~/.claude/skills/` 独立运行）：

| 源头 | 副本 | 怎么同步 |
|---|---|---|
| `落盘台/引擎/`、`落盘台/核心/`、`落盘台/一键落盘.py` | 两个技能的 `scripts/` | `bash 落盘台/同步到技能.sh` |
| `工作台/审查工作台.html` 里的规则与文本积木 | `rules/规则库.json`＋`scripts/prompt_parts.py` | `python3 skills/contract-review-sop/scripts/抽取工作台.py 工作台/审查工作台.html` |

**别两边各改一半。** 规则卡认准一个源头改（要么 JSON，要么工作台），改完往另一边同步。

---

## 1. 规则卡：这套东西的心脏

### 1.1 一张卡长什么样

```json
{
  "id": "T4-1",
  "pack": "T4",
  "t": "起租日不得自签订日起算",
  "when": "土地、房屋、场地、设备租赁",
  "sig": "'自签订之日起租'而标的尚未交付",
  "ask": "改为'实际交付之日起'；免租期安排与'先付后用'的关系。",
  "act": "【修订】",
  "fail": "标的已实际占用的除外。",
  "failReal": "标的已实际占用的除外。"
}
```

| 字段 | 写什么 | 写不好会怎样 |
|---|---|---|
| `id` | 卡号，**前缀必须等于 `pack`**（CORE 包的卡以 `G` 开头） | 重号 → 装配时后一张顶掉前一张，静默丢卡 |
| `pack` | 属于哪个包 | 写了不存在的包 → 这张卡永远装不进来 |
| `t` | 标题，一句话说清是什么问题 | —— |
| `when` | **什么样的文件才适用**。装配后模型逐条自问"本件是不是这种情况" | 写太宽 → 到处误报 |
| `sig` | **在文本里长什么样**。越具体越好，最好直接是能被眼睛扫到的原句片段 | 写成抽象概念 → 模型认不出来 |
| `ask` | 批注的实际用语，可给几个变体。**≤40 字、一条一个问题** | 写成长篇说理 → 客户 Word 里全是墙 |
| `act` | 【修订】还是【批注】还是【补充】 | 该修订的写成批注 → 客户得自己动手 |
| `fail` | ★ **什么情况下这条不适用、照套会怎么误导** | **省了它 → 模型照套，提出错误意见。这是最容易偷懒也最贵的一个字段** |
| `failReal` | 与 `fail` 同步（页面读它，装配器读 `fail`） | 不同步 → 页面与命令行口径不一致 |

**规则库里不留「出处」字段。** 早先每张卡带一个 `src` 记着"这条是从哪份文件总结出来的"，
它不进生成的 prompt、只在页面上显示一行——但一百多条出处摆在一起，等于把一间机构的业务构成
和客户类型全画出来了。已整体删除，**不要再加回来**。

### 1.2 为什么分包，而不是一次全上

138 张卡如果每件都全量套用，会有两个后果：模型在不适用的条目上耗掉判断力（规则封顶，
真问题反而看不见）；把不该提的意见提出来（客户觉得你不懂业务）。

所以分三层，**装配时只带用得上的进场**：

- **CORE 通用核**（15 张）：无条件适用，几乎不会误报。金额验算、附件三对照、份数、
  主体名称、日期推演、法规真伪、交叉引用、管辖……这些每件都要查。
- **S 情形包**（8 个）：按交易实际情况装。多主体、框架两层、补充变更终止、涉政府或村集体、
  涉招投标、对方模板、涉外币、涉不动产。
- **T 类型包**（10 个，最多装一个）：按合同类型装。工程施工、设备采购、技术服务、租赁、
  投资框架、金融借款、公司治理、函件承诺、安全生产、结算变更。

**装配宁滥勿缺**：漏装一个情形包 = 那一整包的硬伤全漏。多装一个包只是多读几条规则，
而且每条都带 `fail` 告诉模型何时不适用。

### 1.3 加一张卡（最常见的动作）

判断路径：

```
遇到一个"上次就该发现"的问题
  └─ 每一件（不分类型）都该查吗？ ── 是 → CORE，id 用 G16、G17…
      └─ 否：只在某种交易情形下才查？ ── 是 → 对应 S 包
          └─ 否：只在某种合同类型里才查？ ── 是 → 对应 T 包
              └─ 都不是：本行业独有的一类合同 → 新建 T11/T12（见 1.5）
```

写卡时按这个顺序想，别跳：

1. **先写 `sig`** —— 上次那份合同里，这个问题**具体长什么样**？照着写一句**同形但虚构**的示例：
   句式、关键词照旧，主体名去掉，具体数字一律换成 `X` `N` `【】`。
   写不出具体样子的，说明你还没想清楚，先别加。
   ⚠️ **不要把来件原句直接粘进来**——那是客户的文件内容。规则卡是要分发的，
   `sig` 只需要「长什么样」，不需要「原文是什么」。
2. **再写 `fail`** —— 什么情况下这条**不该**提？想不出例外的，多半是 `when` 写太宽了。
3. **最后写 `ask`** —— 这条命中时，批注实际会怎么写？照着客户能看懂的口吻写，≤40 字。

改完必跑：

```bash
python3 skills/contract-review-sop/scripts/查规则库.py
```

它查重号、查 id 与包对不上、查孤儿卡、查必填字段为空、查 `fail`/`failReal` 不同步。
**有 ❌ 就别用**——最隐蔽的一种坏法是卡号重复，装配时静默丢卡，你不会收到任何报错。

### 1.4 改一张卡

```bash
python3 skills/contract-review-sop/scripts/assemble.py --card G4     # 先看现在怎么写的
```

三种常见的改法：

- **误报多** → 收窄 `when`，或把误报的情形写进 `fail`。**优先改 `fail`，不要直接删卡**——
  删掉就再也想不起来了，写进 `fail` 反而让模型学会了分辨。
- **漏报** → `sig` 写得太抽象，换成实际见过的原句片段；或者根本没装到那个包，
  去 `rules/识别线索.json` 给这个包加触发词。
- **批注口吻不对** → 只改 `ask`。这是最安全的改动，不影响命中逻辑。

### 1.5 加一个类型包（换行业时要做的事）

工具包出厂带的 10 个类型包是通用合同类型。你这行有自己的一类合同（医药的临床试验协议、
互联网的数据处理协议、影视的版权许可、外贸的 FOB/CIF 条款……），就加一个：

1. `rules/规则库.json` 的 `packs` 里加一项：
   ```json
   { "k": "T11", "label": "类型｜临床试验协议", "desc": "药物/器械临床试验相关合同。" }
   ```
2. 往 `cards` 里加 `T11-1`、`T11-2`…（id 前缀必须是 `T11-`）。
3. `rules/识别线索.json` 的 `type` 里加触发词：
   ```json
   "T11": ["临床试验", "受试者", "伦理委员会", "知情同意", "研究者手册", "SAE"]
   ```
   这些词只用来"扫一眼说像不像"，**不做判断**——装不装仍由模型决定。
4. 跑 `查规则库.py`，再跑一次装配试试：
   ```bash
   python3 skills/contract-review-sop/scripts/assemble.py --type T11 --out /tmp/试.md
   ```
5. 想让工作台页面也有这个选项：把新库同步进 HTML（见 3.2）。

### 1.6 拿自己的稿子"喂"规则库（推荐的长期做法）

这套卡本来就是这么攒出来的：**不是坐着想规则，是回头看自己改过的合同**。

一个可持续的节奏：

1. 手边留一个"这次审出来的问题"清单（落盘块本身就是现成的）。
2. 攒够十来份，问自己两件事：
   - 哪几类问题**反复出现**？→ 值得成为一张卡。
   - 哪几条现有卡**这次误报了**？→ 把这次的情形写进那张卡的 `fail`。
3. 让 Claude 帮你干这件事，一句话就够：

   > 这是我最近改过的 8 份合同的落盘块。对照 `rules/规则库.json`，
   > 告诉我：（1）哪些问题现有卡片没覆盖，建议加成什么卡；
   > （2）哪些卡片这几次没命中或误报了，`when`/`sig`/`fail` 该怎么调。
   > 加卡按 CLAUDE.md 1.3 的字段要求写，改完跑 `查规则库.py`。

4. **脱敏是你的责任，而且要按「谁都推不出来」的标准做**：
   - `sig`/`ask` 里的示例句一律写成同形虚构版，具体数字换成 `X`/`N`/`【】`；
   - 不写主体名、项目名、真实金额（尤其注册资本、合同总价这种能在公开登记里反查的）；
   - 不写"这条来自哪份文件"，也不要统计自己的稿子（"我的批注平均多少字"这类
     也是对外可推断的信息）；
   - **条号只在引法律时写实的**（"《公司法》第 46 条"没问题）；引来件条款一律写成
     "第 X 条第 Y 项"，别留真实条号。
   规则库是会被分发的文件——今天写进去的客户条款，明天就在朋友的电脑里。
   **写完跑一次 `python3 脱敏自检.py`**，它的 L8 专门抓"引号里又长又带具体数字"的片段
   （中文直角引号「」也查，所有文本文件都查——提示词正文常年藏在 `prompt_parts.py` 里；
   句子里提到某部法规也不再整句免检，只把《…》挖掉再判）；
   L9 抓乡镇区县与园区名——`sig` 写成 `××镇` 而 `ask` 忘了改，就是靠它兜住的。
   L8／L9 误伤了正常内容（公开范本条号、自编示例）才加进 `脱敏白名单.txt`；
   真是客户原文的，改原文，别加白名单。白名单只在"整句相等"或"白名单条目本身≥12 字"
   时才放行，短条目不会顺带放行包含它的长句。

> 说明：这里说的"训练"不是训练模型权重，是**改规则库这个文本文件**。
> 改完立刻生效，不需要重启、不需要重新装技能（技能是从这个 JSON 现读的）。

---

## 2. 别的可调项

### 2.1 错别字判词表 `rules/易混词.json`

```json
{ "dup_ok": ["一一", "个个", "某某", …],
  "pairs":  [{"wrong": "签定", "right": "签订", "level": "sure", "why": "签订 才是法定用语"}, …] }
```

- `dup_ok`：**允许**重复的词，防止"某某""一一"被当成衍字。见到新的误报就往这里加。
- `pairs`：`level: sure` 直接出【修订】，`level: check` 出【批注】请核实。
- 见到新的错法（尤其你这行的专有名词近形字）就加一条，`typo.py` 立刻就认。

### 2.2 触发词 `rules/识别线索.json`

`--scan` 用它数词频，告诉你"这份文件像哪个包"。**命中只说明"像"，不等于"是"**，
装不装由模型判断。换行业主要就是改这张表的 `type` 部分。

### 2.3 固定口径 `配置.json`

署名、署名缩写、工作时段、交付文件名后缀、我方默认立场。
`python3 skills/contract-review-sop/scripts/config.py` 打印当前生效值**和它是从哪读到的**。

**这是唯一来源**：命令行脚本从它读，落盘台页面也通过 `/api/state` 从它读——
页面上不留任何人的默认值。往 `ui.html` 里写死署名或作息，是历史上真踩过的坑
（发出去的包里带着上一个人的姓氏缩写和作息表）。

查找顺序：环境变量 `CONTRACT_REVIEW_CONFIG` → 沿目录向上找 `配置.json`（工具包里跑就是它）
→ `~/.contract-review/配置.json`（技能装出去之后落到这份）。
**在工具包里跑就改工具包那份，装出去之后改家目录那份。**

---

## 3. 常规维护动作

### 3.1 改了落盘引擎

```bash
python3 skills/contract-review-sop/scripts/自检.py 某份真实来件.docx
```

它会造出"没有 commentsExtensible""没有 people""整份来件一条批注都没有""同段两条修订"
等变体，把平时跑不到的"新建部件"路径都走一遍。**五个变体全 PASS 再推：**

```bash
bash 落盘台/同步到技能.sh
```

**体检 C0 挂了是脚本的问题，不是文件的问题**——去修脚本（下一份来件也受益），
绝不手写交付件的 XML 去凑。

### 3.2 改了规则卡，要让工作台页面也跟上

工作台把规则库内嵌在 HTML 第一个 `const BUILTIN = {…};` 那一行（单行 JSON）。同步办法：

```bash
python3 - <<'EOF'
import json
from pathlib import Path
p = Path("工作台/审查工作台.html"); lines = p.read_text(encoding="utf-8").split("\n")
lib = json.load(open("skills/contract-review-sop/rules/规则库.json", encoding="utf-8"))
i = next(k for k, l in enumerate(lines) if l.startswith("const BUILTIN = {"))
lines[i] = "const BUILTIN = " + json.dumps(lib, ensure_ascii=False, separators=(",", ":")) + ";"
p.write_text("\n".join(lines), encoding="utf-8")
print("已同步", len(lib["cards"]), "张卡")
EOF
```

反方向（在页面上改完卡片、存成 `rules.js`）：

```bash
python3 skills/contract-review-sop/scripts/抽取工作台.py 工作台/审查工作台.html
```

页面也支持临时挂外部规则库：把 `rules.js` 放在 HTML 同目录即可；
命令行对应的是 `assemble.py --rules 那个文件`。

### 3.3 改了工作台里的 prompt 文字

工作台 HTML 是那些文字的**源头**，`scripts/prompt_parts.py` 是抽出来的副本。改完跑抽取器。
抽取器遇到没见过的 `${...}` 插值会**直接报错**（而不是悄悄抽错），这时在
`抽取工作台.py` 的 `SUB` 表里补一条映射。

有一处例外：A/B 轨那句话（"本件按 A 类／B 类……"）在 `assemble.py` 里另有一份，
改了 HTML 记得也改它。

---

## 4. 给 Claude 的工作约定

在这个目录里工作时：

1. **改规则卡之后必跑** `python3 skills/contract-review-sop/scripts/查规则库.py`，
   有 ❌ 就修完再交。
2. **要把工具包发给别人之前必跑** `python3 脱敏自检.py`，有 ❌ 就修完再发。
   它专抓肉眼会漏的那几类：预填值里的人名与作息、真实金额、私人路径、运行产物。
3. **改了引擎之后必跑** `自检.py`，五个变体全 PASS 才算改完。
4. **卡片迁包时**（改 `pack`），`id` 前缀要一起改——这是历史上真踩过的坑：
   批量改写脚本若在"重新编号"之前按新号去匹配卡片，会命中同号的旧卡，把不相干的卡改坏。
   **先改号、后改内容，或者改完用 `查规则库.py` 对一遍。**
5. **规则库是要分发的文件，按「谁都推不出来」的标准写**：往 `sig`/`ask` 里写示例时，
   句式关键词照旧，主体写"某某公司""××项目"，具体数字一律换成 `X`/`N`/`【】`。
   **不要把来件原句直接粘进卡片**——那是客户的合同内容；卡片只需要「长什么样」。
   也不要加"出处"字段、不要统计用户自己的稿子。
6. **不要替用户降低标准**：C0–C7 体检挂了就不交付，别手写 XML 糊过去；
   落不上的条目如实列出来，别猜着往客户文件里写。
7. 用户提出的判断口径修正，**落回规则库**，不要只在当次对话里迁就。

---

## 5. 这套东西的来历与边界

- 规则卡出自一批实际审查稿的反向校准，**已去除全部主体、项目、行业专属内容**，
  留下的是"什么样的条款有什么问题、该怎么提"。
- 出厂的 138 张卡覆盖通用合同问题。**你这行的专门知识需要你自己往里加**——
  这不是缺陷，是设计：一个装满别人行业规则的库，对你只是噪音。
- 判断仍然是人的。这套东西替你做的是"手不抖"的部分：机械扫描、准确落盘、交付前体检。
  **交付前自己读一遍。**
